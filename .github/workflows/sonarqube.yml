name: SonarQube Analysis

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sonarqube:
    name: SonarQube Code Analysis
    runs-on: ubuntu-latest
    env:
      TEST_DB_PATH: /home/runner/work/test.db
      DATABASE_URL: file:/home/runner/work/test.db
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for better analysis

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install system deps (ffmpeg, tesseract)
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg tesseract-ocr tesseract-ocr-por tesseract-ocr-eng

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Setup test database
        run: npx prisma db push --skip-generate

      - name: Run tests with coverage
        run: npm run test:coverage

      - name: Run linter
        run: npm run lint
        continue-on-error: true

      # Option 1: SonarCloud (free for public repos, recommended)
      # Requires SONAR_TOKEN secret in repo settings.
      # Create at: https://sonarcloud.io > My Account > Security > Generate Tokens
      # Then add as repo secret: Settings > Secrets and variables > Actions > New secret
      - name: SonarCloud Scan
        if: ${{ env.SONAR_TOKEN != '' }}
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Skip SonarCloud (no token)
        if: ${{ env.SONAR_TOKEN == '' }}
        run: |
          echo "::warning title=SonarCloud::SONAR_TOKEN secret not configured. Skipping SonarCloud scan."
          echo "To enable: create a token at https://sonarcloud.io and add it as SONAR_TOKEN in repo Settings > Secrets > Actions"

      # Option 2: Self-hosted SonarQube (uncomment if running own server)
      # - name: SonarQube Scan
      #   uses: SonarSource/sonarqube-scan-action@master
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      #     SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
